# MinisSkills
Individual skill modules under `MinisSkills/`.
Each module contains `SKILL.md` and `scripts/run.py`.
Use `scripts/run.py` to invoke the skill's `run(payload)`.
## Run example
```bash
python3 MinisSkills/assault/scripts/run.py
```
